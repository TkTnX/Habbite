# Habbite - Приложение для отслеживания привычек и здоровья

## 📋 Описание проекта

**Habbite** — это полнофункциональное веб-приложение для отслеживания привычек, здоровья и физических показателей. Приложение помогает пользователям вести здоровый образ жизни, отслеживать свой вес, потребление жидкости, выполнять задачи и анализировать свой прогресс.

## 🎯 Основной функционал

### 1. **Аутентификация и авторизация**
- Регистрация новых пользователей
- Вход в систему с запоминанием сессии
- Восстановление пароля через email
- Установка нового пароля
- JWT-аутентификация с использованием access и refresh токенов
- Защищенные маршруты с middleware авторизации

### 2. **Управление задачами (Tasks)**
- Создание задач с указанием даты, названия, описания и цвета
- Просмотр задач в календарном виде
- Отметка задач как выполненных/невыполненных
- Редактирование существующих задач
- Удаление задач
- Фильтрация задач по датам в календаре

### 3. **Отслеживание веса**
- Добавление записей о весе с привязкой к дате
- Отображение графика изменения веса за последние 30 дней
- Расчет индекса массы тела (ИМТ)
- Отображение текущего ИМТ с подсказками о категории (недостаточный вес, нормальный, избыточный и т.д.)
- Визуализация прогресса достижения целевого веса

### 4. **Отслеживание жидкости**
- Добавление записей о выпитой жидкости с указанием типа напитка и объема
- Создание пользовательских типов напитков с выбором цвета
- Аналитика потребления жидкости за неделю
- Круговая диаграмма распределения типов напитков
- Отслеживание водного баланса

### 5. **Аналитика и статистика**
- Графики изменения веса за последние 30 дней
- Аналитика потребления жидкости за неделю
- Визуализация ИМТ с индикацией категории
- Отображение прогресса к цели по весу
- Визуализация данных в виде графиков и диаграмм

### 6. **Профиль пользователя**
- Редактирование личных данных (имя, фамилия, email)
- Загрузка аватара пользователя
- Настройка целевого веса
- Указание роста и даты рождения
- Выбор пола (мужской/женский)
- Начальная настройка профиля при регистрации (многошаговая форма)

### 7. **Интерфейс**
- Адаптивный дизайн
- Темная и светлая темы с переключением
- Боковая панель навигации
- Календарь для управления задачами
- Современный UI с использованием Material-UI

## 🛠 Технологический стек

### Frontend
- **React 19.2.0** — библиотека для создания пользовательского интерфейса
- **TypeScript** — типизированный JavaScript для надежности кода
- **Vite** — быстрый сборщик и dev-сервер
- **React Router 7.13.0** — маршрутизация в приложении
- **Material-UI (MUI) 7.3.7** — компоненты интерфейса
- **@mui/x-charts 8.26.0** — библиотека для графиков и диаграмм
- **Zustand 5.0.11** — управление состоянием приложения
- **TanStack React Query 5.90.20** — управление серверным состоянием и кэшированием
- **Axios 1.13.4** — HTTP-клиент для запросов к API
- **React Toastify 11.0.5** — уведомления для пользователя
- **Lucide React 0.563.0** — иконки
- **Sass 1.97.3** — препроцессор CSS
- **js-cookie 3.0.5** — работа с cookies
- **use-debounce 10.1.0** — debounce для оптимизации

### Backend
- **Node.js** — серверная платформа
- **Express 5.2.1** — веб-фреймворк для Node.js
- **MongoDB 7.0.0** — NoSQL база данных
- **Mongoose 9.1.5** — ODM для MongoDB
- **JWT (jsonwebtoken 9.0.3)** — аутентификация через токены
- **Argon2 0.44.0** — хеширование паролей
- **Multer 2.0.2** — загрузка файлов
- **Resend 6.9.2** — отправка email для восстановления пароля
- **CORS 2.8.6** — настройка CORS для кросс-доменных запросов
- **Cookie-parser 1.4.7** — парсинг cookies
- **dotenv 17.2.3** — управление переменными окружения

## 🗄 Модели данных

### User (Пользователь)
- `firstName` — имя (обязательное)
- `lastName` — фамилия
- `email` — email (обязательное, уникальное)
- `avatar` — путь к аватару
- `password` — хешированный пароль
- `provider` — провайдер аутентификации (по умолчанию "credentials")
- `weightGoal` — целевой вес
- `height` — рост
- `birthday` — дата рождения
- `gender` — пол (male/female)
- `userDrinks` — ссылки на записи о напитках
- `weights` — ссылки на записи о весе

### Task (Задача)
- `title` — название задачи (обязательное)
- `text` — описание задачи (обязательное)
- `user` — ссылка на пользователя
- `date` — дата задачи в формате строки (обязательное)
- `color` — цвет задачи (обязательное)
- `isCompleted` — статус выполнения (по умолчанию false)
- `createdAt`, `updatedAt` — временные метки

### Weight (Вес)
- `weight` — значение веса
- `user` — ссылка на пользователя
- `createdAt`, `updatedAt` — временные метки

### Drink (Напиток)
- `name` — название напитка (обязательное)
- `color` — цвет для отображения (обязательное)

### UserDrink (Запись о выпитом напитке)
- `ml` — объем в миллилитрах (обязательное)
- `drink` — ссылка на тип напитка
- `user` — ссылка на пользователя
- `createdAt`, `updatedAt` — временные метки

## 🔌 API Endpoints

### Аутентификация (`/auth`)
- `POST /auth/registration` — регистрация нового пользователя
- `POST /auth/login` — вход в систему
- `POST /auth/refresh` — обновление access токена
- `POST /auth/logout` — выход из системы (требует авторизации)
- `POST /auth/send-email` — отправка email для восстановления пароля
- `PATCH /auth/new-password` — установка нового пароля

### Пользователь (`/user`)
- `GET /user/me` — получение данных текущего пользователя (требует авторизации)
- `PATCH /user` — обновление профиля пользователя (требует авторизации, поддерживает загрузку изображения)

### Задачи (`/task`)
- `GET /task` — получение всех задач пользователя (требует авторизации)
- `POST /task` — создание новой задачи (требует авторизации)
- `PATCH /task/:id` — обновление задачи (требует авторизации)
- `PATCH /task/change-status/:id` — изменение статуса задачи (требует авторизации)
- `DELETE /task/:id` — удаление задачи (требует авторизации)

### Напитки (`/drink`)
- `GET /drink` — получение всех типов напитков
- `POST /drink` — создание нового типа напитка
- `POST /drink/user` — добавление записи о выпитом напитке (требует авторизации)

### Вес (`/weight`)
- `POST /weight` — добавление записи о весе (требует авторизации)

## 🚀 Установка и запуск

### Требования
- Node.js (версия 18 или выше)
- MongoDB (локально или MongoDB Atlas)
- npm или yarn

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в директории `backend` со следующим содержимым:
```env
PORT=5000
DB_URL=mongodb://localhost:27017/habbite
JWT_SECRET_KEY=your-secret-key-here
CLIENT_URL=http://localhost:5173
RESEND_API_KEY=your-resend-api-key
```

4. Запустите сервер:
```bash
npm start
```

Сервер будет запущен на `http://localhost:5000`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в директории `frontend` (если требуется):
```env
VITE_API_URL=http://localhost:5000
```

4. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### Production сборка

Для сборки frontend для production:
```bash
cd frontend
npm run build
```

Собранные файлы будут в директории `frontend/dist`

## 🔐 Безопасность

- Пароли хешируются с использованием Argon2
- JWT токены для аутентификации
- Refresh токены хранятся в httpOnly cookies
- Middleware для защиты маршрутов
- Валидация данных на сервере
- CORS настроен для безопасности

## 📊 Особенности реализации

- **State Management**: Использование Zustand для глобального состояния и React Query для серверного состояния
- **Типизация**: Полная типизация TypeScript на frontend
- **Оптимизация**: Debounce для поиска, кэширование запросов через React Query
- **UI/UX**: Material-UI компоненты, адаптивный дизайн, темная/светлая темы
- **Валидация**: Валидация форм на клиенте и сервере
- **Обработка ошибок**: Централизованная обработка ошибок через middleware

## 🎨 Основные страницы

1. **Главная страница (`/`)** — дашборд с аналитикой, графиками веса, воды и информацией о теле
2. **Страница задач (`/tasks`)** — календарь с задачами
3. **Настройки (`/settings`)** — редактирование профиля
4. **Страница начала (`/start`)** — начальная настройка профиля для новых пользователей
5. **Вход (`/auth/login`)** — страница входа
6. **Регистрация (`/auth/register`)** — страница регистрации
7. **Восстановление пароля (`/auth/reset-password`)** — запрос на восстановление
8. **Новый пароль (`/auth/new-password`)** — установка нового пароля

